#include"shell.h"
/**
 * main - Entry point of the programm
 * printf: function that prints data to the stdout(standard output)
 * Return: 0 on success and non-zero on error
*/

int main(void)
{
	printf("Am writing a program that passes the Betty check\n");

	return (0);
}
